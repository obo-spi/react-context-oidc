language: node_js
node_js:
  - "8"
  - "9"

cache: yarn

before_install:
  - yarn add jest react babel-preset-react-app
install: yarn
script:
  - yarn test

jobs:
  include:
    - stage: npm release
      if: tag IS present
      node_js: "8"
      script: yarn compile
      before_deploy:
        - cd dist
      deploy:
        provider: npm
        email: "$NPM_EMAIL"
        api_key: "$NPM_TOKEN"
        skip_cleanup: true
        on:
          tags: true

# variables d'env
env:
  global:
    - NODE_ENV=production